<VirtualHost *:80>
    ServerName {{host}}
    ServerAlias  {{host}}
    DocumentRoot "{{root_dir}}/web"
    <Directory "{{root_dir}}/web">
        AllowOverride All
    </Directory>
</VirtualHost>